# Learned corrections

Sentences flagged, with the fixes that worked. Appended by the `humanize` skill when a
rewrite gets rejected. This file outranks both `voice.md` and the shipped pattern list:
each entry is something already gotten wrong once.
